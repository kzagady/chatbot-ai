services:
  - type: web
    name: chatbot-ai
    env: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: OPENAI_API_KEY
        value: ENTER_YOUR_KEY_IN_RENDER_DASHBOARD
